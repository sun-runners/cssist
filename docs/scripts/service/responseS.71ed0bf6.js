'use strict';

angular.module("idistApp")
	.service('responseS', function($rootScope, $state, $q, $timeout, storageS){

		// this.alertInstagramCheckResponse = function(response){
    //   if(response.data){
    //     if(response.data.login==false) alert('인스타그램 아이디, 비밀번호를 확인해주세요.');
    //   }
    // };

  });
